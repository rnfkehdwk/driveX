# 다른 유저분께 받아올 정보 체크리스트

> 실시간 대화 중이시니 한 번에 다 물어보면 좋습니다.
> 답변 받은 후 이 파일에 기록 → SETUP_GUIDE.md 업데이트.

---

## 🎯 핵심 질문 5가지

### Q1. MCP 서버 패키지
**질문**: "filesystem 작업용으로 어떤 MCP 서버 쓰세요? npm 패키지 이름이나 GitHub 링크 부탁드립니다."

**답변**:
```
(여기에 받은 답변 기록)
```

후보:
- `@modelcontextprotocol/server-filesystem` (공식, stdio 전용 가능성)
- `desktop-commander` (커뮤니티, shell+filesystem 통합)
- `mcp-remote` 래퍼
- 기타: ___________

---

### Q2. HTTP 노출 방식
**질문**: "MCP 서버를 어떻게 인터넷에 노출하셨나요? Cloudflare Tunnel? ngrok? 직접 포트포워딩?"

**답변**:
```
(여기에 받은 답변 기록)
```

후보:
- Cloudflare Tunnel (무료, HTTPS 자동)
- ngrok (유료 추천)
- 직접 포트포워딩 + Let's Encrypt
- 기타: ___________

---

### Q3. 인증 방식
**질문**: "Anthropic 서버가 MCP에 붙을 때 인증은 어떻게 거셨어요? OAuth? API key? Cloudflare Access?"

**답변**:
```
(여기에 받은 답변 기록)
```

후보:
- OAuth 2.0 (가장 안전, 구현 복잡)
- Bearer 토큰 (단순, 헤더로 전달)
- Cloudflare Access service token
- 인증 없이 URL 비밀로 (위험)
- 기타: ___________

---

### Q4. 배포 자동화
**질문**: "'배포까지 클로드가 한다'고 하셨는데, 구체적으로 어떻게 셋업하셨나요? shell MCP로 명령어 실행하는 거예요, 아니면 git push 감지해서 자동 배포되는 거예요?"

**답변**:
```
(여기에 받은 답변 기록)
```

후보:
- shell/command MCP로 직접 명령 실행
- git push → webhook → CI/CD
- 자작 watcher 스크립트
- 기타: ___________

---

### Q5. 호스팅 위치
**질문**: "MCP 서버를 PC에 띄우셨어요, NAS나 별도 서버에 띄우셨어요?"

**답변**:
```
(여기에 받은 답변 기록)
```

후보:
- 개발 PC에 직접 (PC 켜져 있어야 함)
- NAS / 자체 서버 (24/7 가능)
- VPS (월 비용 발생)
- 기타: ___________

---

## 📌 추가로 물어볼 만한 것

### Q6. 보안 사고 경험
**질문**: "운영하시면서 토큰 노출이나 보안 이슈 있으셨나요? 어떻게 대처하셨어요?"

**답변**:
```
```

### Q7. 비용
**질문**: "월 운영 비용 얼마나 나오세요? (Cloudflare 유료 플랜, 도메인, MCP API 호출 등)"

**답변**:
```
```

### Q8. 셋업 시간
**질문**: "처음 셋업하실 때 얼마나 걸리셨어요? 가장 헤매신 부분이 뭐예요?"

**답변**:
```
```

### Q9. 운영 PC vs 개발 PC 분리
**질문**: "MCP가 접근하는 코드 폴더가 운영 코드랑 분리되어 있나요? 모바일 실수로 운영에 직접 영향 줄 위험은 어떻게 막으셨어요?"

**답변**:
```
```

### Q10. Claude.ai 설정 화면 팁
**질문**: "claude.ai에서 Custom Connector 등록할 때 헷갈렸던 부분 있으세요? URL 형식이라든가 헤더 설정이라든가."

**답변**:
```
```

---

## ✏️ 답변 받은 후 할 일

1. [ ] 위에 답변 기록
2. [ ] `SETUP_GUIDE.md`의 "TBD" 부분을 정확한 값으로 업데이트
3. [ ] 그분 방식과 다른 부분이 있으면 가이드 수정
4. [ ] PC 앞에 가서 Phase 1부터 진행

---

## 💬 질문 던질 때 한 번에 보낼 메시지 (복붙용)

```
혹시 셋업 자세한 거 좀 여쭤봐도 될까요? 비슷하게 셋업하려고 정리 중인데 5개만 알려주시면 큰 도움이 될 것 같아요.

1. filesystem 작업용 MCP 서버는 어떤 패키지 쓰세요? (npm 이름이나 GitHub 링크)
2. 인터넷 노출은 뭘로 하셨어요? (Cloudflare Tunnel / ngrok / 직접 포트포워딩 / 기타)
3. 인증은 어떻게 거셨어요? (OAuth / Bearer 토큰 / Cloudflare Access / 기타)
4. 배포 자동화는 shell MCP로 명령 실행이세요, 아니면 git push 감지하는 webhook 방식이세요?
5. MCP 서버는 개발 PC에 띄우셨어요, 아니면 NAS/별도 서버예요?

답변 가능하신 것만 알려주셔도 충분합니다. 감사합니다!
```
